# 🚀 Инструкция по деплою на Heroku

## Подготовка

1. Установите Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli

2. Войдите в Heroku:
```bash
heroku login
```

## Создание приложения

1. Создайте новое приложение на Heroku:
```bash
heroku create kein-bot
```

2. Установите переменные окружения:
```bash
heroku config:set BOT_TOKEN=8208172752:AAGf6N9eLTr0Qe6TUSDkp_ojhAQdp24Z1Dw
```

3. Проверьте переменные:
```bash
heroku config
```

## Деплой

1. Инициализируйте git (если еще не сделано):
```bash
git init
```

2. Добавьте файлы:
```bash
git add .
```

3. Сделайте коммит:
```bash
git commit -m "Initial commit"
```

4. Добавьте Heroku remote:
```bash
heroku git:remote -a kein-bot
```

5. Отправьте код:
```bash
git push heroku main
```

## Проверка

1. Проверьте логи:
```bash
heroku logs --tail
```

2. Откройте приложение в браузере:
```bash
heroku open
```

## Обновление бота

После внесения изменений:

```bash
git add .
git commit -m "Update bot"
git push heroku main
```

## Отладка

Если что-то пошло не так:

1. Проверьте логи:
```bash
heroku logs --tail
```

2. Проверьте переменные окружения:
```bash
heroku config
```

3. Перезапустите приложение:
```bash
heroku restart
```

## Важные замечания

- Токен бота должен быть установлен в переменных окружения Heroku
- Приложение должно слушать на порту, который задает Heroku (переменная PORT)
- Procfile указывает Heroku, как запускать приложение
