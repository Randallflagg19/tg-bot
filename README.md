# KeinBot - Telegram Bot с характером

Telegram бот с живой личностью и интересными ответами.

## 🚀 Быстрый старт

### Локальная разработка

1. Установите зависимости:

```bash
npm install
```

2. Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_here
PORT=3000
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-your_deepseek_key_here
```

**🔑 Как получить DeepSeek API ключ (бесплатно!):**
1. Перейдите на https://platform.deepseek.com/api_keys
2. Войдите или зарегистрируйтесь
3. Создайте новый ключ (начинается с `sk-`)
4. Скопируйте и вставьте в `.env`

**💡 Про бесплатный лимит:** DeepSeek дает бесплатный кредит новым пользователям ($5-10), этого хватит на тысячи сообщений!

**📖 Подробная инструкция:** см. [SETUP_AI.md](./SETUP_AI.md)

**Альтернатива (Hugging Face):** Если хотите использовать Hugging Face вместо DeepSeek:
```env
AI_PROVIDER=huggingface
HF_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxx
HF_MODEL=tiiuae/falcon-7b-instruct
```
Токен Hugging Face: https://huggingface.co/settings/tokens

**⚠️ Без AI ключа:** Бот будет работать, но без умных ответов (только команды /help, /joke).

3. Запустите бота в режиме разработки:

```bash
npm run dev
```

4. Откройте Telegram и найдите бота:

   - По имени: **@KeinAssBot**
   - Или по ссылке: https://t.me/KeinAssBot

5. Напишите боту сообщение или отправьте команду `/start`

**Важно:** Бот работает локально на вашем компьютере. Вы общаетесь с ним через Telegram мессенджер, а не через терминал!

### Сборка и запуск

```bash
npm run build
npm start
```

## 📦 Деплой на Heroku

1. Установите Heroku CLI и войдите:

```bash
heroku login
```

2. Создайте новое приложение:

```bash
heroku create kein-bot
```

3. Установите переменные окружения:

```bash
heroku config:set BOT_TOKEN=8208172752:AAGf6N9eLTr0Qe6TUSDkp_ojhAQdp24Z1Dw
heroku config:set AI_PROVIDER=huggingface
heroku config:set HF_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxx
heroku config:set HF_MODEL=tiiuae/falcon-7b-instruct
# Опционально:
# heroku config:set DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

4. Отправьте код:

```bash
git init
git add .
git commit -m "Initial commit"
git push heroku main
```

5. Проверьте логи:

```bash
heroku logs --tail
```

## 🎯 Особенности

- ✅ **AI-ответы с характером Кейна** — по умолчанию Hugging Face (бесплатный лимит), опционально DeepSeek
- ✅ **Персонаж Кейна** - ютуб-блогер, снимающий обзоры на моды для Майнкрафт
- ✅ **Живая личность** - саркастичный, юмористичный, с чувством абсурдного юмора
- ✅ **История разговора** - помнит контекст общения с каждым пользователем
- ✅ **Команды**: /start, /help, /joke
- ✅ **Готов к деплою на Heroku**
- ✅ **Express сервер** для health checks
- ✅ **Гибкий выбор AI-провайдера** — Hugging Face, DeepSeek, локальный Ollama (можно расширять)

## 📝 Команды бота

- `/start` - начать общение
- `/help` - показать справку
- `/joke` - получить шутку

## 💬 Что умеет бот

Бот использует AI для умных ответов в характере Кейна:

- **Умные ответы** - Hugging Face Inference API (по умолчанию) + поддержка других провайдеров
- **История разговора** - помнит контекст общения с каждым пользователем
- **Характер Кейна** - саркастичный, юмористичный, с чувством абсурдного юмора
- **Обращение к сообществу** - называет пользователей частью "лампового сообщества"
- **Самоирония** - шутит над своими провалами и псевдонимами
- **Отсылки к Майнкрафту** - упоминает моды, спидраны, карты от подписчиков
- **Скрытность** - намекает на свою скрытность, но не раскрывает детали
- **Отсылки к пошиву одежды** - иногда упоминает свою коллекцию одежды
- **Музыка** - изредка упоминает свою музыку

## 🔑 Настройка AI-провайдера

### DeepSeek (рекомендуется, бесплатный старт) ⭐

**Почему DeepSeek:**
- 🆓 **Бесплатный кредит** для новых пользователей ($5-10)
- 💰 **Очень дешево** после исчерпания лимита (~$0.14 за 1M токенов)
- ⚡ **Быстро** и качественно
- 🔌 **Простая настройка** - один API ключ

**Как настроить:**
1. Перейдите на https://platform.deepseek.com/api_keys
2. Войдите или зарегистрируйтесь
3. Создайте новый API ключ (начинается с `sk-`)
4. Добавьте в `.env`:

```env
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-ваш_ключ_здесь
```

**📖 Подробная инструкция:** см. [SETUP_AI.md](./SETUP_AI.md)

### Hugging Face (альтернатива)

Если хотите использовать Hugging Face:
1. Перейдите на https://huggingface.co/settings/tokens
2. Создайте `Fine-grained token` с правами `Read` + `Inference`
3. Добавьте в `.env`:

```env
AI_PROVIDER=huggingface
HF_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxx
HF_MODEL=tiiuae/falcon-7b-instruct
```

Можно использовать другую публичную модель (например, `meta-llama/Meta-Llama-3-8B-Instruct`).

> 💡 Можно свободно переключаться между провайдерами, изменив `AI_PROVIDER` в `.env`

## 🔧 Технологии

- **Telegraf** - библиотека для работы с Telegram Bot API
- **Hugging Face / DeepSeek / Ollama** - гибкий выбор AI-провайдера
- **Express** - веб-сервер для Heroku
- **TypeScript** - типизация кода
- **Node.js** - среда выполнения

## 📌 Важные замечания

- Храните токен бота в безопасности
- Не коммитьте `.env` файл в git
- Для Heroku используйте переменные окружения через `heroku config:set`

## 🐛 Отладка

### Локальная отладка

Когда бот запущен локально (`npm run dev`), вы увидите логи в терминале:

- Сообщения об успешном запуске
- Ошибки (если есть)
- Информацию о полученных сообщениях

Чтобы остановить бота, нажмите `Ctrl+C` в терминале.

**Важно:** Бот работает локально на вашем компьютере. Вы общаетесь с ним через Telegram мессенджер (@KeinAssBot), а не через терминал!

### Отладка на Heroku

Для просмотра логов на Heroku:

```bash
heroku logs --tail
```

## 💡 Как работает бот локально?

Когда вы запускаете бота локально:

1. Бот подключается к Telegram API через ваш интернет
2. Telegram отправляет сообщения боту через **polling** (опрос сервера)
3. Бот обрабатывает сообщения локально на вашем компьютере
4. Вы общаетесь с ботом через Telegram мессенджер (не через терминал!)
5. Изменения в коде автоматически перезагружаются (благодаря `tsx watch`)
