# 🔑 Настройка AI для бота (DeepSeek)

## 🎯 Краткая инструкция (5 минут)

**Код уже готов!** Нужно только получить API ключ и добавить его в `.env`.

### Шаг 1: Получите DeepSeek API ключ (бесплатно!)

1. **Перейдите на** https://platform.deepseek.com/api_keys
2. **Войдите или зарегистрируйтесь** (можно через Google/GitHub)
3. **Нажмите "Create new secret key"** или "Создать новый ключ"
4. **Скопируйте ключ** (начинается с `sk-`)
   - ⚠️ **Важно:** Ключ показывается только один раз! Сохраните его сразу.

**💡 Про бесплатный лимит:**
- DeepSeek дает **бесплатный кредит** новым пользователям (обычно $5-10)
- Этого хватит на **тысячи сообщений** для бота
- После исчерпания нужно пополнить баланс (но это очень дешево - ~$0.14 за 1M токенов)

### Шаг 2: Добавьте ключ в `.env` файл

Создайте файл `.env` в корне проекта (если его нет) и добавьте:

```env
BOT_TOKEN=ваш_токен_бота
PORT=3000
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-ваш_ключ_здесь
```

**Пример:**
```env
BOT_TOKEN=8208172752:AAGf6N9eLTr0Qe6TUSDkp_ojhAQdp24Z1Dw
PORT=3000
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-proj-abc123def456...
```

### Шаг 3: Перезапустите бота

Если бот запущен, остановите его (`Ctrl+C`) и запустите снова:

```bash
npm run dev
```

### Шаг 4: Проверьте работу

1. **Напишите боту** сообщение в Telegram
2. **Проверьте логи** в терминале - должно быть:
   ```
   🤖 Используется DeepSeek как AI провайдер
   🤖 Бот запущен!
   ✅ AI-ответы включены (DeepSeek)
   ```
3. **Бот должен ответить** с характером Кейна! 🎉

## 💰 Про стоимость и бесплатный лимит

### Бесплатный старт
- **Новые пользователи** получают бесплатный кредит ($5-10)
- Этого хватит на **тысячи сообщений** (примерно 10,000-50,000 токенов)
- Для личного бота этого обычно хватает надолго

### После исчерпания бесплатного лимита
- **Очень дешево**: ~$0.14 за 1M входных токенов, ~$0.28 за 1M выходных
- **Пример:** 1000 сообщений ≈ $0.01-0.05 (копейки!)
- Бот экономит токены: помнит только последние 10 сообщений

### Как проверить баланс
1. Зайдите на https://platform.deepseek.com
2. Перейдите в раздел "Billing" или "Баланс"
3. Там увидите текущий баланс и историю использования

## ✅ Преимущества DeepSeek

- **🆓 Бесплатный старт** - кредит для новых пользователей
- **💰 Дешево** - в 10-20 раз дешевле OpenAI
- **⚡ Быстро** - быстрая обработка запросов
- **🎯 Качество** - отличное качество ответов (сравнимо с GPT-3.5)
- **🔌 Совместимость** - использует OpenAI-совместимый API (код уже готов!)

## ⚠️ Важные замечания

- **🔒 Безопасность**: Никогда не публикуйте API ключ в git или публичных местах
- **📝 Модель**: Бот использует `deepseek-chat` - основную модель DeepSeek
- **🚫 Без API ключа**: Бот будет работать, но без AI-ответов (только команды /help, /joke)
- **🔄 Переключение**: Можно легко переключиться на Hugging Face, изменив `AI_PROVIDER`

## Проверка работы

После добавления ключа вы должны увидеть в логах:

```
🤖 Бот запущен!
✅ AI-ответы включены (DeepSeek)
```

Если видите:
```
⚠️  AI-ответы отключены (нет DEEPSEEK_API_KEY)
```

Значит ключ не найден. Проверьте:
1. Правильно ли указан ключ в `.env`
2. Нет ли пробелов в начале/конце ключа
3. Перезапущен ли бот после добавления ключа

## 📊 Технические детали

- **Модель**: `deepseek-chat` (основная модель DeepSeek)
- **Стоимость**: 
  - Входные токены: ~$0.14 за 1M
  - Выходные токены: ~$0.28 за 1M
- **История разговора**: Бот помнит до 10 последних сообщений (экономит токены)
- **Максимальная длина ответа**: 150 токенов (короткие ответы с характером)
- **API URL**: `https://api.deepseek.com` (используется автоматически)

## 🐛 Отладка проблем

### Проблема: "AI-ответы отключены"

**Решение:**
1. Проверьте, что файл `.env` существует в корне проекта
2. Убедитесь, что ключ указан правильно (начинается с `sk-`)
3. Проверьте, нет ли пробелов в начале/конце ключа
4. Убедитесь, что указан `AI_PROVIDER=deepseek`
5. Перезапустите бота после изменений

### Проблема: "Insufficient Balance" (недостаточно баланса)

**Решение:**
1. Зайдите на https://platform.deepseek.com
2. Проверьте баланс в разделе "Billing"
3. Если баланс = 0, пополните его (минимум обычно $1-5)
4. Или создайте новый аккаунт для нового бесплатного кредита

### Проблема: Бот не отвечает или отвечает ошибкой

**Решение:**
1. Проверьте логи в терминале - там будет детальная информация
2. Убедитесь, что интернет работает
3. Проверьте, что API ключ не истек (ключи не истекают, но могут быть удалены)
4. Попробуйте перезапустить бота

### Проблема: Бот отвечает, но не с характером Кейна

**Решение:**
1. Это нормально - AI может иногда "сбиваться"
2. Попробуйте написать `/start` заново - это сбросит историю
3. Характер задается системным промптом (см. `src/character.ts`)

## Сравнение с OpenAI

| Параметр | DeepSeek | OpenAI |
|----------|----------|--------|
| Стоимость | Дешевле | Дороже |
| Скорость | Быстрее | Медленнее |
| Качество | Отличное | Отличное |
| API | Совместим с OpenAI | Стандартный |

## Альтернативы

Если не хотите использовать DeepSeek, можно:
- Вернуться на OpenAI (нужно изменить код)
- Использовать другие AI-сервисы (Anthropic, Google, etc.)
- Использовать локальные модели
- Оставить бота без AI (он будет работать, но с простыми ответами)